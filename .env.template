# Environment Variables Template for GetGlow

# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to version control!

# ============================================
# REQUIRED FOR ALL DEPLOYMENTS
# ============================================

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.aws.neon.tech/dbname?sslmode=require
PGDATABASE=neondb
PGHOST=your-host.aws.neon.tech
PGPORT=5432
PGUSER=your_user
PGPASSWORD=your_password

# OpenAI API
OPENAI_API_KEY=sk-proj-...your-key-here...

# Session Security (Generate a new random string!)
# Run in terminal: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
SESSION_SECRET=your-long-random-secret-here

# Environment
NODE_ENV=development
# Use 'production' for Vercel deployment

# ============================================
# CLOUDFLARE R2 STORAGE (Optional but Recommended)
# ============================================
# Get these from: https://dash.cloudflare.com/ > R2 > API Tokens

R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=getglow-images
R2_PUBLIC_DOMAIN=pub-xxxxx.r2.dev
# ☝️ Or use your custom domain like: cdn.yourdomain.com

# ============================================
# REPLIT AUTH (Only if using Replit)
# ============================================
REPL_ID=your-repl-id
REPL_OWNER_ID=your-owner-id
REPLIT_DOMAINS=your-repl.replit.dev,127.0.0.1:5000
ISSUER_URL=https://replit.com/oidc

# ============================================
# NOTES
# ============================================
# 
# Local Development (without R2):
# - Just fill in DATABASE_URL, OPENAI_API_KEY, SESSION_SECRET
# - Images will be stored locally in /uploads
# 
# Vercel Deployment (with R2):
# - Fill in ALL variables above (except Replit Auth)
# - Add R2 variables for cloud storage
# - Images will be stored in Cloudflare R2
# 
# Security:
# - NEVER commit your .env file
# - ROTATE your OpenAI API key if it was exposed
# - Use different SESSION_SECRET for each environment
#
